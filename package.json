{
  "name": "beatuploader",
  "private": true,
  "description": "Root manifest so Neon config-as-code tooling (neon.ts) resolves locally instead of walking up to $HOME.",
  "dependencies": {
    "@neon/config": "^1.5.0",
    "@neon/env": "^1.3.4"
  }
}
